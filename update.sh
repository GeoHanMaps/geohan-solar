#!/bin/bash
# Bu script yerini deploy.sh'ye bırakmıştır.
# Kullanım:
#   ./deploy.sh <sunucu_ip>            — güncelleme
#   ./deploy.sh <sunucu_ip> --rollback — önceki versiyona dön
#   ./deploy.sh <sunucu_ip> --setup    — ilk kurulum
echo "Bu script artık kullanılmıyor. Lütfen deploy.sh kullanın:"
echo "  ./deploy.sh <sunucu_ip>"
exit 1
